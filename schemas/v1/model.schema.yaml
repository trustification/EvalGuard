$schema: http://json-schema.org/draft/2020-12/schema
$id: https://github.com/evalguard/evalguard/schemas/v1/task_definition.schema.yaml
title: TaskDefinition
description: Schema for a model evaluation task, based on lm-eval report data plus user-added metadata.
type: object
properties:
  id:
    type: string
    description: Unique task identifier.
  name:
    type: string
    description: Human-readable name of the task.
  description:
    type: string
    description: Optional detailed description of the task.
  category:
    type: string
    description: Optional category of the task, e.g. 'question_answering', 'language_modeling'.
  tags:
    type: array
    description: Optional tags for the task, e.g. domain, difficulty.
    items:
      type: string
  languages:
    type: array
    description: Optional list of languages relevant to the task.
    items:
      type: string
required:
  - id
  - name
additionalProperties: false 